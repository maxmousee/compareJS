class Base64JSON {
    constructor(text, isBase64) {
      this.text = text;
      this.isBase64 = isBase64;
    }
}

class ComparableJSON {
    constructor(left, right) {
        this.left = left;
        this.right = right;
    }
}